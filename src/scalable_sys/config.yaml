llm:
  backend: rag
  rag:
    db_path: "data/nobel.kuzu"
    # Behavior Flags
    use_exemplars: true
    use_self_refine: true
    use_postprocess: true
    # Caching Strategy
    cache_text2cypher: true
    cache_maxsize: 256
    cache_ttl_seconds: 0
